# App directory

This folder is for feature specific components (e.g., a list of members, message input, etc.) basically a parent component with various other components inside.
This is **NOT** a page, but a component that can be used and imported into pages.