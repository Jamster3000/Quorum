# Routes directory

This folder contains all the pages of the application, which inherits various components from the `lib/components` directory.

Folders which are named inside of () brackets are a trick with SvelteKit to create dynamic routes where the folder name ]
isn't included in the URL. 