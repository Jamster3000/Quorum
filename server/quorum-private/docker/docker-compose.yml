services:
  surrealdb:
    image: surrealdb/surrealdb:v3.1.5
    user: root
    command: start --user root --pass root --bind 0.0.0.0:8000 surrealkv:///data/quorum.db
    ports:
      - "8000:8000"
    volumes:
      - surreal_data:/data

volumes:
  surreal_data: